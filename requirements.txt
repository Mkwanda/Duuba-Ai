# Core dependencies for Duuba-AI web deployment
# Frozen from cod environment - only necessary packages

# Machine Learning & Object Detection
tensorflow==2.10.1
tf-models-official>=2.10

# Image Processing
opencv-python-headless==4.8.0.76
Pillow==9.5.0
numpy==1.23.5

# Web Framework
streamlit>=1.20
PyYAML>=5.0

# Protobuf (required for TF Object Detection API)
protobuf==3.19.6

# Model Download Utilities (optional - for Google Drive downloads)
requests==2.28.2
gdown

# Note: tf-models-official includes the complete Object Detection API with compiled protos
# opencv-python-headless is used for cloud deployments (no GUI dependencies)