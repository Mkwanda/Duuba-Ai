# Core dependencies for Duuba-AI web deployment
# Frozen from cod environment - only necessary packages

# Machine Learning & Object Detection
tensorflow==2.10.1
object-detection>=0.0.3,<0.0.4


# Image Processing
opencv-python==4.7.0.72
opencv-python-headless==4.8.0.76
Pillow==9.5.0
numpy==1.23.5

# Web Framework
streamlit>=1.20
PyYAML>=5.4.1

# Protobuf (required for TF Object Detection API)
protobuf==3.19.6

# Model Download Utilities (optional - for Google Drive downloads)
requests==2.28.2
gdown

<<<<<<< HEAD
# Note: opencv-python-headless is used instead of opencv-python for cloud deployments
# It excludes GUI dependencies which aren't needed in web servers
=======
# Note: opencv-python-headless is used instead of opencv-python for cloud deployments
# It excludes GUI dependencies which aren't needed in web servers
>>>>>>> parent of 07c96f8 (Fix object detection import - use tf-models-official instead of object-detection)
